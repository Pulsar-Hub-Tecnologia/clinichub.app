export enum AppRoutes {
  AUTH = "/auth",
  ACCOUNT = "/account",
  USER = "/user",
  WORKSPACE = "/workspace",
}

export enum RoutesIBGE {
  ESTADOS = "/estados",
  CIDADES = "/municipios"
}